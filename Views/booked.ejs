<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
   <link rel="stylesheet" href="/css/profilestyle.css">
</head>
<body>
     <%- include('partials/header.ejs') %>
    <main>
    <% if (rides.length > 0) { %>
    <% rides.forEach(ride => {  const booking = ride.bookings.find(b => b.userId === userId);  %>
         <li class="ride-item">
         <strong><%= ride.location %></strong> =><%= ride.destination %><br>
    <% }) %>
    <% } else { %>
    <div class="container">
        <img id="map" src="/img/map.jpg" alt="map">
        <h1>Your future travel plans will appear here.</h1>
        <p>Find the perfect ride from thousands of destinations, or publish to share your travel costs.</p>
    </div>
<% } %>
</main>
</body>
</html>